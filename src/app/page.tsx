'use client';

import { CourseSelector } from '@/components/course/CourseSelector';

export default function HomePage() {
  // Toujours afficher le sélecteur de cours sur la page d'accueil
  return <CourseSelector />;
}
